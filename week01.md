# Week 1

## 1. Python REPL

Python은 기본적으로 REPL 환경에서 실행할 수 있다.
REPL은 다음을 의미한다.

- Read : 입력 읽기
- Evaluate : 계산 수행
- Print : 결과 출력
- Loop : 반복

Python 프롬프트에서 보이는 `>>>` 기호는 Python 인터프리터가 사용자의 입력을 기다리고 있는 상태를 의미한다.
REPL의 특징은 마지막 수행 결과를 바로 출력한다는 것이다.

---

## 2. Expression과 Statement

### Expression

하나의 값으로 치환될 수 있는 코드를 의미한다.

예

```python
3 + 5
```
결과

```8```

### Statement

Python에게 어떤 동작을 수행하도록 하는 코드 한 줄을 의미한다.

예

```val = 30```

이 코드는 값으로 치환되지 않으므로 expression이 아니다.

Statement는 실행 시 None을 반환하지만 REPL에서는 None을 따로 출력하지 않는다.

## 3. 할당 연산자 (=)

= 는 할당 연산자(assignment operator)이다.
왼쪽에는 변수, 오른쪽에는 값이 온다.

예

```val = 30```

Python에서는 변수 선언과 할당을 하나의 과정으로 처리한다.

다른 언어에서는

```int x```

처럼 변수 타입을 먼저 선언하지만 Python은 동적 타입 언어(dynamic typing language)이기 때문에 변수에 타입이 없다.
변수는 단지 객체를 가리키는 이름표(reference) 역할을 한다.

## 4. 데이터 타입 (Data Type)

기본적인 데이터 타입

- int : 정수
- float : 실수
- str : 문자열

예

```x = 10
y = 3.14
z = "hello"
```

이처럼 Python 변수는 어떤 타입의 객체도 가리킬 수 있다.

## 5. 연산자 (Operators)

Python에서는 다양한 연산자를 사용할 수 있다.

사칙연산

\+ 덧셈
\- 뺄셈
\* 곱셈, asterisk(아스테리스크)
\/ 나눗셈

### 연산 순서 (precedence)

괄호 > 곱셈/나눗셈 > 덧셈/뺄셈

예

```3 + 5 * 2```

결과

```13```

## 6. 나눗셈 연산

Python에는 여러 가지 나눗셈 연산이 있다.

### 일반 나눗셈
/

```4 / 2```

결과

```2.0```

### 정수 나눗셈 (floor division)
//

```12 // 10```

결과

```1```

### 음수의 경우

```-12 // 10```

결과

```-2```

### 나머지 연산 (modulo)
%

```10 % 3```

결과

```1```

## 7. print 함수

print() 함수는 값을 출력하는 함수이다.

함수는 소괄호 () 를 사용하며 괄호 안에 argument(인자)를 넣는다.

예

```print("Hello")```

숫자도 출력할 수 있다.

```print(5)```

결과

```5```

문자열과 표현식 차이

```print(3 + 5)```

결과

```8```

```print("3+5")```

결과

```3+5```

문자열은 계산되지 않고 그대로 출력된다.

여러 인자 출력
```print("3+5 =", 3+5)```

결과

```3+5 = 8```

콤마 , 는 각 인자를 공백으로 구분하여 출력한다.

## 8. 변수 값 교환

기존 방식

```tmp = x
x = y
y = tmp```

Python 방식

```x, y = y, x```

이는 튜플(tuple)과 언패킹(unpacking) 개념을 이용한 것이다.

## 9. 다중 할당

Python에서는 여러 값을 동시에 할당할 수 있다.

```x, y = 1, 4```

또는

```a = 1, 2, 3, 4```

이 경우 a 는 튜플이 된다.

```(1, 2, 3, 4)```

튜플의 값은 인덱스를 이용하여 접근할 수 있다.

```print(a[0])```

결과

```1```

## 10. 증강 할당 연산자

기존 방식

```x = x + 5```

증강 연산자 사용

```x += 5```
정리

이번 수업에서는 다음 내용을 학습하였다.

- Python REPL 환경
- Expression과 Statement
- 변수와 할당
- 데이터 타입
- 연산자와 연산 순서
- print 함수
- 변수 값 교환
- 다중 할당
